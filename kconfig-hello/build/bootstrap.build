project = kconfig-hello

using version
using config

# Note that you don't need to (but may still want to) do this in your
# applications since kconfig is a standard pre-installed module.
#
if ($build.mode != 'skeleton')
  using kconfig

using test
using install
using dist
